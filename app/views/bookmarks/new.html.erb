<div class=container>
  <%= simple_form_for [@list, @bookmark] do |f|%>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit %>
  <% end %>
  <%= link_to "Back to list", list_path(@list) %>
</div>
